// Day n: title
// Part One
const { input } = require("./input.js");
const { run } = require("../../util/run.js");

run(() => {
  // TODO: add code here

  return { output: null, expected: null };
});
